#!/bin/bash
set -e
echo "oran-dhcp-gen v2.2.3 installed."
echo ""
echo "What's new in 2.2.2:"
echo "  * Multi-line ISC TLV emission. Vendor option chains with 3+"
echo "    sub-options now render with each sub-option on its own line"
echo "    (matching the visual structure of hand-edited dhcpd.conf)."
echo "    1-2 sub-options stay single-line. Wire bytes unchanged."
echo ""
echo "Earlier in 2.2.x:"
echo "  * 2.2.1 - Spec-compliant 0x86 byte (auto from class.protocol)"
echo "  * 2.2.0 - lease_profiles, ca_ra_profiles, pool-scoped lease times"
echo ""
echo "Usage:"
echo "  oran-dhcp-gen oran_dhcp.yaml                          # generate only"
echo "  oran-dhcp-gen oran_dhcp.yaml --deploy --restart       # deploy"
echo ""
echo "Example YAML model: /usr/share/oran-dhcp-gen/oran_dhcp.yaml.example"
